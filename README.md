# 🧩 Anagram Game

Приложение-игра на составление слов из букв.  
**Фронтенд:** React + Vite  
**Бэкенд:** Node.js + Express + MongoDB

---

## 🚀 Запуск проекта локально

### 1️⃣ Установка зависимостей

В корне проекта выполните:

```bash
npm install
```

Перейдите в папку `server` и установите зависимости для сервера:

```bash
cd server
npm install
```

---

### 2️⃣ Создание `.env` для сервера

В папке `server/` создайте файл `.env` и добавьте следующие переменные:

```env
MONGO_URI=ваш_адрес_подключения_MongoDB
PORT=5000
JWT_SECRET=ваш_секрет_для_JWT
```
---

### 3️⃣ Запуск сервера

```bash
cd server
node index.js
```

Или (если в `package.json` сервера есть скрипт):

```bash
cd server
npm run start
```

---

### Создание 4️⃣ `.env` для клиента

В корне проекта (рядом с `package.json`) создайте файл `.env`:

```env
VITE_API_URL=http://localhost:5000
```

---


### 5️⃣ Запуск клиента

В корне проекта выполните:

```bash
npm run dev
```

---

## 📁 Структура проекта

```
anagramma/
├─ public/
├─ src/
├─ server/
│  ├─ routes/
│  ├─ models/
│  ├─ middleware/
│  ├─ db.js
│  ├─ index.js
│  └─ .env  ← создаётся пользователем
├─ package.json
└─ README.md
```

---

## 📝 Возможности игры

- Авторизация / регистрация (JWT)
- Таймер раунда
- Таблица топ-игроков
- Подсчёт очков
- Серверная валидация

---